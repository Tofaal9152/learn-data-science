import numpy as np

arr= np.array([1,2,3,4,5])
# print(np.sum(arr))
# print(np.min(arr))
# print(np.max(arr))
# print(np.mean(arr))
# print(np.var(arr))

# standard deviation of the array
print(np.std(arr)) 
# variance of the array => Calculates the variance, which is the average of the squared differences from the mean.
print(np.var(arr))

